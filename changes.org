
* 0.2 (Q1 2026) (configure, test infra, Haskell/Zig)
# start to use Claude

** languages
- support for highlighting Haskell (using the generic AST highlighter)
- support for Zig too

** build infra
- configure (mostly copied from osemgrep)
  (Thx Claude)

** test infra
- start to use Testo and basic highlighting regression testing
  (Thx Claude)

** docs
- resume codemap.nw
  (thx Claude)

** internals
- switch to semgrep-libs and semgrep-pfff-langs submodules instead
  of relying on ~/.opam

* 0.1 (Q3 2025) (Dockerfile and GHA, OCaml5 ready)

** testinfra
- Dockerfile and docker.yml GHA (GitHub Actions) CI working for
  OCaml 4.14.2 and 5.2.1

* gamma (Q? 2020) (extracted from pfff)

** languages
- support for a few languages using tree-sitter-based parsers

** internals
- extracted codemap in its own repo after the integration/move of pfff
  in semgrep
  
* beta (Q? 2013) (codegraph integration, Java/JS?)

** languages
- support also JS? Java?

** features
- leverage graph_code.marshall generated by codegraph for red/green
  dependencies visualization from codemap
- deps-based navigation (deps-in, deps-out, deps-inout) and selection of
  files

* alpha  (Q? 2012) (first proto, PHP/OCaml/C)

** features
- first prototype as part of pfff at Facebook with most of the features
  (treemap, thumbnails, navigation)

** languages
- highlighting for OCaml and PHP, and C?

** docs
- codemap.nw
