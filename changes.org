
* 0.2 (Q1 2026) (configure)
# start to use Claude

** build infra
- configure (mostly copied from osemgrep)

* 0.1 (Q3 2025) (Dockerfile and GHA, OCaml5 ready)

** testinfra
- Dockerfile and docker.yml GHA (GitHub Actions) CI working for
  OCaml 4.14.2 and 5.2.1

* gamma (Q? 2020) (extracted from pfff)

** internals
- extracted codemap in its own repo after the integration/move of pfff
  in semgrep
  
* beta (Q? 2013) (codegraph integration)

** features
- leverage graph_code.marshall generated by codegraph for red/green
  dependencies visualization from codemap
- deps-based navigation (deps-in, deps-out, deps-inout) and selection of
  files

* alpha  (Q? 2012) (first proto, PHP/OCaml/C)

** features
- first prototype as part of pfff at Facebook with most of the features
  (treemap, thumbnails, navigation)
- highlighting for OCaml and PHP

** docs
- codemap.nw
