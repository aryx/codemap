# No need for much here as codemap now by default, thx to the gitignore library,
# also process .gitignore !

# ignore submodules
#/semgrep-pfff-langs
#/semgrep-libs

# generated by hand, but in AI-days they could be considered almost
# boilerplate so let's skip them here
/semgrep-pfff-langs/**/Parse_*_tree_sitter.ml

/semgrep-pfff-langs/**/orig_spec/
/semgrep-pfff-langs/**/grammar.js
